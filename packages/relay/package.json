{
  "name": "@arcveil/relay",
  "version": "0.1.0",
  "description": "Submits shielded withdrawals so a fresh address never has to hold gas to be paid.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@arcveil/bridge": "workspace:*",
    "viem": "^2",
    "zod": "^4"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^3",
    "wrangler": "^4"
  },
  "engines": {
    "node": ">=20"
  }
}
